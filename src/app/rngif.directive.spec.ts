import { RngifDirective } from './rngif.directive';

describe('RngifDirective', () => {
  it('should create an instance', () => {
    const directive = new RngifDirective();
    expect(directive).toBeTruthy();
  });
});
